build:
  command: bundle exec asciidoctor-pdf -r asciidoctor-kroki -a chapter-signifier= -a kroki-default-format=png -a allow-uri-read -a kroki-max-uri-length=8000 --sourcemap
  publish: true
  keepSource: true
assembly:
  profile: pdf-requirements
  insert_start_page: false
  root_level: 1
  sectionMergeStrategy: fuse
  doctype: book
  attributes:
    pdf-theme: ./pdf-theme.yml
    sectnums: ""
    xrefstyle: full
    outlinelevels: '2'
    toclevels: '3'
