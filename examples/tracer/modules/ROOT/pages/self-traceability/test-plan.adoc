= Test Plan
:keywords: traceability, test plan, tests, verification, coverage
:traceability-links: true
:traceability-graph: true
:traceability-style: table
:traceability-order: target-id
:traceability-collapsible: true

This document maps the test suite to the requirements it verifies.
Each test file is a traceable `[item]` referencing the requirements it covers.

== Test Coverage

[#TST-001, item, role=test, title="Core Extension Tests"]
--
Test file: `test/v2-unified-item-architecture.test.ts`

Tests the `RequirementsTraceabilityExtension` orchestrator — initialization, processing, query methods, role statistics, lifecycle.
Covers the public API surface of the main extension class.
Includes verbatim block handling (listing, literal, and backtick code spans).

verifies:REQ-087[]
verifies:REQ-088[]
verifies:REQ-090[]
verifies:REQ-091[]
verifies:REQ-092[]

tracer:links[]

tracer:graph[]
--

[#TST-002, item, role=test, title="Antora Extension Tests"]
--
Test file: `test/antora-extension.test.ts`

Tests the `AntoraTraceabilityExtension` — initialization with default config, preset, config path, and disabled state.
Tests event handler registration, content processing via `contentClassified` event (in-memory only, never touches disk), matrix generation via `sitePublished` event and matrix registration in the content catalog via `contentClassified`, and API access patterns.
Also tests `tracer:outgoing[]`, `tracer:incoming[]`, and `tracer:links[]` macro expansion with configurable display style, sort order, collapsible sections, empty-state messages (`:traceability-empty:`), and opt-in behavior.
Tests incoming macro display styles, sort orders, reverse labels, PDF compatibility, and collapsible support.
Tests `tracer:graph[]`, `tracer:graph-coverage[]`, and `tracer:config-graph[]` macro expansion with configurable Kroki image format and Kroki server URL (extension config option and `KROKI_SERVER_URL` env var override), graph-enabled and graph-disabled modes, including global coverage, explicit target IDs, and header exclusion.
Tests automatic matrix registration in the content catalog during `contentClassified`.
Tests the per-version `graph.json` snapshot registration: snapshot content (items and relationships with scope, no `pubUrl`), gating by `generateMatrices`, empty-graph versions, and in-place refresh of a committed copy.
Tests partial file processing including item registration from partials, view URL as sourceFile, and macro expansion in Pass 2.
Tests cross-module and cross-component xref prefix generation.
Tests `git_describe` injection into `site.keys` and `asciidoc.attributes`, including the non-git fallback and absent-section handling.

verifies:REQ-250[]
verifies:REQ-041[]
verifies:REQ-044[]
verifies:REQ-045[]
verifies:REQ-047[]
verifies:REQ-048[]
verifies:REQ-051[]
verifies:REQ-052[]
verifies:REQ-159[]
verifies:REQ-055[]
verifies:REQ-063[]
verifies:REQ-142[]
verifies:REQ-143[]
verifies:REQ-064[]
verifies:REQ-065[]
verifies:REQ-066[]
verifies:REQ-067[]
verifies:REQ-070[]
verifies:REQ-221[]
verifies:REQ-222[]
verifies:REQ-161[]
verifies:REQ-162[]
verifies:REQ-100[]
verifies:REQ-102[]
verifies:REQ-249[]
verifies:REQ-104[]
verifies:REQ-108[]
verifies:REQ-256[]
verifies:REQ-257[]
verifies:REQ-258[]
verifies:REQ-259[]
verifies:REQ-110[]
verifies:REQ-116[]
verifies:REQ-117[]
verifies:REQ-119[]
verifies:REQ-144[]
verifies:REQ-145[]
verifies:REQ-146[]
verifies:QA-058[]
verifies:QA-060[]
verifies:REQ-163[]
verifies:REQ-164[]
verifies:REQ-165[]
verifies:REQ-166[]
verifies:REQ-244[]
verifies:REQ-252[]
verifies:REQ-253[]
verifies:REQ-254[]
verifies:REQ-255[]

tracer:links[]
--

[#TST-003, item, role=test, title="CLI Tests"]
--
Test file: `test/cli.test.ts`

Tests the Commander-based CLI — process command (valid content, empty content, invalid paths, malformed macros), matrix command (generation, empty graph), validate command (invalid graph state), export command (empty graph), stats command (empty statistics), preset commands (invalid names, init, listing), help command, and next-id command (sequential ID generation, padding preservation, prefix matching).

verifies:REQ-056[]
verifies:REQ-150[]

tracer:links[]
--

[#TST-004, item, role=test, title="ConfigLoader Tests"]
--
Test file: `test/config-loader.test.ts`

Tests the `ConfigLoader` — initialization, built-in preset listing and loading, preset structure validation (including pairwise matrix definitions), configuration loading from file, missing file handling, invalid YAML handling, role and relation validation, matrix definition retrieval, configuration merging (including label overrides), and error messages for invalid configurations and preset names.

verifies:REQ-083[]
verifies:REQ-084[]
verifies:REQ-085[]
verifies:REQ-086[]
verifies:REQ-170[]
verifies:REQ-171[]
verifies:REQ-172[]
verifies:REQ-173[]
verifies:REQ-223[]
verifies:REQ-224[]
verifies:REQ-175[]
verifies:REQ-177[]

tracer:links[]
--

[#TST-005, item, role=test, title="Graph and API Tests"]
--
Test file: `test/graph-and-api.test.ts`

Tests the `TraceabilityGraph` (extended queries: merge, getRelatedItems, getItemsWithRelationTo, getRelationshipsByRoles, findPath, getImpactAnalysis) and graph visualization methods (`toDot`, `toVegaLite`, `toConfigDot`).
Tests graph integrity validation including circular reference detection (direct, indirect, self-referencing cycles, bidirectional pair exclusion), dangling reference detection with source location and expected-role hints, and invalid relation type reporting with allowed-relations hints.
Tests bidirectional relationship merge.
Also tests `RequirementsTraceabilityExtension` API methods (processFiles, validate, export, coverage, config access, preset management, lifecycle).

verifies:REQ-053[]
verifies:REQ-160[]
verifies:REQ-109[]
verifies:REQ-219[]
verifies:REQ-220[]
verifies:REQ-176[]

tracer:links[]
--

[#TST-006, item, role=test, title="MatrixGenerator Tests"]
--
Test file: `test/matrix-generator.test.ts`

Tests the `MatrixGenerator` — basic matrix generation with empty graph, matrix generation with default and named configurations, role-based matrix rows and columns, coverage percentage calculation and status determination (`done`/`partial`/`missing`), coverage relation filtering, CSV export, HTML export, JSON export, coverage report generation, and edge cases (no matching items, missing titles, missing source files, unknown matrix names).

verifies:REQ-037[]
verifies:REQ-038[]
verifies:REQ-039[]
verifies:REQ-042[]
verifies:REQ-043[]
verifies:REQ-105[]
verifies:REQ-128[]
verifies:REQ-130[]
verifies:REQ-167[]
verifies:REQ-168[]
verifies:REQ-169[]
verifies:REQ-178[]
verifies:REQ-113[]
verifies:REQ-114[]
verifies:REQ-115[]

tracer:links[]
--

[#TST-007, item, role=test, title="Neo4jExporter Tests"]
--
Test file: `test/neo4j-exporter.test.ts`

Tests the `Neo4jExporter` — empty graph CSV export, items and relationships in CSV, all attributes in export, special character escaping, empty graph Cypher export, items and relationships in Cypher, `includeContent` option, `includeAllAttributes` option, output directory creation, multiple role types, and export result counts and file paths.

tracer:links[]
--

[#TST-008, item, role=test, title="Relationship Link Substitution Tests"]
--
Test file: `test/relationship-link-substitution.test.ts`

Tests the in-memory regex substitution that replaces relationship macros with Asciidoctor xrefs.
Covers same-page xref generation, cross-page xref generation with basename path resolution, dangling reference handling, non-relationship text preservation, hyphenated/dotted IDs, missing sourceFile fallback, and surrounding text preservation.

tracer:links[]
--

[#TST-009, item, role=test, title="Query Command Tests"]
--
Test file: `test/query-command.test.ts`

Tests the `query` CLI subcommand end-to-end — reverse (inbound relationships, empty results, unknown ID), impact (connected items, disconnected items), isolated (relationship-less items, role filter), orphaned (superseded items without incoming functional links), path (chain output, no path, unknown ID), and the `--json` flag (valid JSON, empty array).
Spawns the compiled CLI binary against a temporary fixture directory.

verifies:REQ-136[]
verifies:REQ-137[]
verifies:REQ-138[]
verifies:REQ-139[]
verifies:REQ-140[]
verifies:REQ-141[]
verifies:REQ-227[]

tracer:links[]
--

[#TST-010, item, role=test, title="Vale Extension Tests"]
--
Test file: `test/antora-vale-extension.test.ts`

Tests the bundled Vale prose-lint extension with fake `vale` and `asciidoctor` executables on PATH — page and partial findings with source path and line, severity gating (at-or-above fails the build, below is logged), opt-in behavior with no `.adoc` files, missing-executable failure messages, and that a multi-sentence source line produces no Vale finding.

verifies:REQ-151[]
verifies:REQ-152[]
verifies:REQ-154[]
verifies:REQ-155[]
verifies:REQ-156[]
verifies:REQ-153[]

tracer:links[]
--

[#TST-011, item, role=test, title="Supersession Tests"]
--
Test file: `test/supersession.test.ts`

Tests requirement supersession: deriving superseded state and successors from the `supersedes` relation, split and merge support, self-supersession and cycle validation errors, advisory stale-link warnings, orphaned-state derivation, dangling-reference detection with history/functional classification, and that superseded items are excluded from current-item queries and matrices.

verifies:REQ-201[]
verifies:REQ-202[]
verifies:REQ-203[]
verifies:REQ-204[]
verifies:REQ-205[]
verifies:REQ-206[]
verifies:REQ-207[]
verifies:REQ-211[]
verifies:REQ-228[]
verifies:REQ-234[]

tracer:links[]
--

[#TST-012, item, role=test, title="Graph Diff Tests"]
--
Test file: `test/graph-diff.test.ts`

Tests `diffGraphs`: added/removed/unmodified classification, field-level modification reporting, suppression of removed items' relationships, relationship deltas on surviving items, and that a superseded pair appears as removed plus added with the `supersedes` link.

verifies:REQ-212[]
verifies:REQ-213[]
verifies:REQ-214[]
verifies:REQ-225[]
verifies:REQ-226[]
verifies:REQ-216[]

tracer:links[]
--

[#TST-013, item, role=test, title="Item Lifecycle Tests"]
--
Test file: `test/item-lifecycle.test.ts`

Tests the `archive` and `remove` CLI commands end-to-end: moving a superseded block to `superseded.adoc` with content preserved, rejecting non-superseded and unknown IDs, removing an orphaned item after confirmation, declining on a non-y answer, removing an isolated item only after typing the ID, declining on ID mismatch, and rejecting a non-orphaned/non-isolated item.

verifies:REQ-235[]
verifies:REQ-236[]
verifies:REQ-237[]
verifies:REQ-238[]

tracer:links[]
--

[#TST-014, item, role=test, title="Supersession Overview and Render Toggle Tests"]
--
Test file: `test/antora-extension.test.ts` (supersession overview and render toggle)

Tests the generated supersession overview content — totals, per-role statistics, and the dangling-reference worklist — the display-only `renderSuperseded` toggle that omits superseded item blocks from rendered output without affecting the graph, and the full-graph accumulation across components used by the generation passes.

verifies:REQ-229[]
verifies:REQ-230[]
verifies:REQ-231[]
verifies:REQ-232[]
verifies:REQ-233[]
verifies:REQ-239[]

tracer:links[]
--

[#TST-015, item, role=test, title="Documentation Example Validation Tests"]
--
Test file: `test/doc-example-validation.test.ts`

Tests that every `[item]` example in the documentation uses known roles and relation types allowed by the traceability configuration.

verifies:REQ-200[]

tracer:links[]
--

[#TST-016, item, role=test, title="Release Consistency Tests"]
--
Test file: `test/release-check.test.ts`

Tests the release-consistency checker's pure validation logic: a consistent release passes, and a missing tag, missing maintenance branch, missing changelog entry, and nonexistent playbook ref are each reported.

verifies:REQ-245[]
verifies:REQ-246[]
verifies:REQ-247[]
verifies:REQ-248[]

tracer:links[]
--

ifdef::ssembler-backend-pdf[]
== Changelog
[cols="1,2,2,3"]
|===
| Version | Date | Author | Change

| 0.7.0 | 2026-07-28 | Team | Initial PDF release
|===
endif::[]
