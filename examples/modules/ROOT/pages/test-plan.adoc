= Test Plan
:traceability-links: true
:traceability-graph: true
:traceability-style: table
:traceability-order: target-id
:traceability-collapsible: true

This document maps the test suite to the requirements it verifies. Each test file is a traceable `[item]` referencing the requirements it covers.

== Test Coverage

[#TST-001, item, role=test, title="Core Extension Tests"]
--
Test file: `test/v2-unified-item-architecture.test.ts`

Tests the `RequirementsTraceabilityExtension` orchestrator — initialization, processing, query methods, role statistics, lifecycle. Covers the public API surface of the main extension class.

verifies:REQ-001[]
verifies:REQ-002[]
verifies:REQ-003[]
verifies:REQ-004[]
verifies:REQ-005[]
verifies:REQ-029[]
verifies:REQ-030[]
verifies:REQ-034[]

traceability:outgoing[]
traceability:incoming[]

traceability:graph[]
--

[#TST-002, item, role=test, title="Antora Extension Tests"]
--
Test file: `test/antora-extension.test.ts`

Tests the `AntoraTraceabilityExtension` — initialization with default config, preset, config path, and disabled state. Tests event handler registration, content processing via `contentClassified` event, matrix generation via `sitePublished` event, and API access patterns. Also tests `traceability:outgoing[]` and `traceability:incoming[]` macro expansion with configurable display style, sort order, collapsible sections, and opt-in behavior. Tests `traceability:graph[]` and `traceability:graph-coverage[]` macro expansion with graph-enabled and graph-disabled modes, including global coverage, explicit target IDs, and header exclusion.

verifies:REQ-035[]
verifies:REQ-040[]
verifies:REQ-044[]
verifies:REQ-045[]
verifies:REQ-047[]
verifies:REQ-048[]
verifies:REQ-051[]
verifies:REQ-052[]
verifies:REQ-055[]

traceability:outgoing[]
traceability:incoming[]
--

[#TST-003, item, role=test, title="CLI Tests"]
--
Test file: `test/cli.test.ts`

Tests the Commander-based CLI — process command (valid content, empty content, invalid paths, malformed macros), matrix command (generation, empty graph), validate command (invalid graph state), export command (empty graph), stats command (empty statistics), preset commands (invalid names, init, listing), and help command.

verifies:REQ-009[]
verifies:REQ-010[]
verifies:REQ-026[]
verifies:REQ-036[]

traceability:outgoing[]
traceability:incoming[]
--

[#TST-004, item, role=test, title="ConfigLoader Tests"]
--
Test file: `test/config-loader.test.ts`

Tests the `ConfigLoader` — initialization, built-in preset listing and loading, preset structure validation, configuration loading from file, missing file handling, invalid YAML handling, role and relation validation, matrix definition retrieval, configuration merging, and error messages for invalid configurations and preset names.

verifies:REQ-006[]
verifies:REQ-007[]
verifies:REQ-008[]
verifies:REQ-010[]
verifies:REQ-011[]
verifies:REQ-012[]
verifies:REQ-018[]
verifies:REQ-019[]
verifies:REQ-020[]
verifies:REQ-021[]
verifies:REQ-022[]
verifies:REQ-023[]

traceability:outgoing[]
traceability:incoming[]
--

[#TST-005, item, role=test, title="Graph and API Tests"]
--
Test file: `test/graph-and-api.test.ts`

Tests the `TraceabilityGraph` (Section 4 — extended queries: merge, getRelatedItems, getItemsWithRelationTo, getRelationshipsByRoles, findPath, getImpactAnalysis) and graph visualization methods (`toDot`, `toVegaLite`). Also tests `RequirementsTraceabilityExtension` API methods (Section 5 — processFiles, validate, export, coverage, config access, preset management, lifecycle).

verifies:REQ-013[]
verifies:REQ-014[]
verifies:REQ-015[]
verifies:REQ-016[]
verifies:REQ-017[]
verifies:REQ-031[]
verifies:REQ-032[]
verifies:REQ-033[]
verifies:REQ-053[]

traceability:outgoing[]
traceability:incoming[]
--

[#TST-006, item, role=test, title="MatrixGenerator Tests"]
--
Test file: `test/matrix-generator.test.ts`

Tests the `MatrixGenerator` — basic matrix generation with empty graph, matrix generation with default and named configurations, role-based matrix rows and columns, coverage percentage calculation and status determination, coverage relation filtering, CSV export, HTML export, JSON export, coverage report generation, and edge cases (no matching items, missing titles, missing source files, unknown matrix names).

verifies:REQ-008[]
verifies:REQ-029[]
verifies:REQ-037[]
verifies:REQ-038[]
verifies:REQ-039[]
verifies:REQ-042[]
verifies:REQ-043[]

traceability:outgoing[]
traceability:incoming[]
--

[#TST-007, item, role=test, title="Neo4jExporter Tests"]
--
Test file: `test/neo4j-exporter.test.ts`

Tests the `Neo4jExporter` — empty graph CSV export, items and relationships in CSV, all attributes in export, special character escaping, empty graph Cypher export, items and relationships in Cypher, `includeContent` option, `includeAllAttributes` option, output directory creation, multiple role types, and export result counts and file paths.

verifies:REQ-024[]
verifies:REQ-025[]
verifies:REQ-027[]
verifies:REQ-028[]

traceability:outgoing[]
traceability:incoming[]
--

[#TST-008, item, role=test, title="Relationship Link Substitution Tests"]
--
Test file: `test/relationship-link-substitution.test.ts`

Tests the in-memory regex substitution that replaces relationship macros with Asciidoctor xrefs. Covers same-page xref generation, cross-page xref generation with basename path resolution, orphan reference handling, non-relationship text preservation, hyphenated/dotted IDs, missing sourceFile fallback, and surrounding text preservation.

verifies:REQ-010[]
verifies:REQ-046[]

traceability:outgoing[]
traceability:incoming[]
--

ifdef::ssembler-backend-pdf[]
== Changelog
[cols="1,2,2,3"]
|===
| Version | Date | Author | Change

| 0.7.0 | 2026-07-28 | Team | Initial PDF release
|===
endif::[]
