{
  "name": "antora-req-trace-example",
  "version": "0.1.0",
  "description": "Example Antora site demonstrating the Requirements Traceability Extension",
  "private": true,
  "scripts": {
    "build": "npx antora antora.yml",
    "build:clean": "rm -rf _site && npm run build",
    "demo": "npm install && npm run build && echo 'Site built in _site/ directory. Open _site/index.html in your browser.'",
    "demo:open": "npm run demo && open _site/index.html"
  },
  "dependencies": {
    "@asciidoctor/core": "^2.2.7"
  },
  "devDependencies": {
    "antora": "^3.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
