= Example Requirements
:description: Example requirements demonstrating the Requirements Traceability Extension

This page demonstrates how to define requirements using the Requirements Traceability Extension.

== Simple Example

Here's how to define a requirement using the `[req]` block macro:

[req, id=REQ-011, title="Welcome Page"]
====
The example site must have a welcome page that introduces the Requirements Traceability Extension and provides a quick start guide.

**Acceptance Criteria:**

* Welcome page must explain what the extension does
* Welcome page must include a quick start guide
* Welcome page must link to other sections
====

[req, id=REQ-012, title="Requirement Definition"]
====
The example site must demonstrate how to define requirements using the extension's syntax.

**Acceptance Criteria:**

* At least 4 example requirements must be defined
* Each requirement must have a unique ID
* Each requirement must have a title
* Each requirement must have descriptive content
====

[req, id=REQ-013, title="Traceability Linking"]
====
The example site must demonstrate how to create traceability links between requirements, implementations, tests, documents, and designs.

**Acceptance Criteria:**

* At least 4 implementations must be defined
* At least 4 relationships must be created using inline macros
* All relationship types must be demonstrated (satisfies, implements, tests, verifies, documents, addresses, composed-of, depends-on)
====

[req, id=REQ-014, title="Matrix Generation"]
====
The example site must demonstrate matrix generation capabilities.

**Acceptance Criteria:**

* The build must generate Requirements-to-Implementation matrix
* The build must generate Requirements-to-Test matrix
* The build must generate Requirements-to-Design matrix (Phase 2)
* The build must generate Design-to-Implementation matrix (Phase 2)
* The build must generate Full traceability matrix
* Matrices must be generated in HTML format
* Matrices must be generated in CSV format
====

== Design Concepts (Phase 2)

The extension also supports design concepts that can address requirements and be implemented.

[design, id=DES-001, title="Welcome Page Design"]
====
The welcome page design includes:

* Header with extension name and version
* Navigation menu
* Main content area with introduction
* Quick start guide section
* Links to other sections

addresses:REQ-011[]
====

[design, id=DES-002, title="Requirement Definition Component"]
====
The requirement definition component provides:

* Block macro syntax for defining requirements
* Attribute parsing (id, title, status)
* Validation of requirement IDs
* Auto-generation of IDs when not provided

addresses:REQ-012[]
implements:DES-004[]
====

[design, id=DES-003, title="Traceability Linking System"]
====
The traceability linking system enables:

* Inline macro syntax for relationships
* Support for all relationship types (addresses, composed-of, depends-on)
* Bidirectional relationship tracking
* Circular reference detection

addresses:REQ-013[]
composed-of:DES-004[]
implements:DES-004[]
====

[design, id=DES-004, title="Inline Macro Parser"]
====
The inline macro parser handles:

* Parsing inline macros like `implements:REQ-001[]`
* Extracting relationship type and target ID
* Validating referenced nodes exist
* Associating relationships with source nodes
* Supporting all relationship types: satisfies, implements, tests, verifies, documents, addresses, composed-of, depends-on
* Supporting all relationship types: satisfies, implements, tests, verifies, documents, addresses, composed-of, depends-on
====

== Real-World Example: Extension Requirements

The following are the actual requirements from the Requirements Traceability Extension specification.
These demonstrate how the extension can be used to document real-world requirements.

NOTE: Design concepts (Phase 2) are demonstrated in the section above.

[req, id=REQ-001, title="Define Requirements in AsciiDoc"]
====
The system shall allow users to define requirements using custom AsciiDoc syntax.

**Acceptance Criteria:**

* Users can define requirements using a `[req]` block macro
* Requirements can have unique identifiers
* Requirements can have titles and descriptions
* Requirements can have custom attributes (status, priority, etc.)
* Requirement syntax is validated during processing
====

[req, id=REQ-002, title="Establish Traceability Links"]
====
The system shall allow users to establish traceability links between requirements, implementations, tests, documents, and design concepts.

**Acceptance Criteria:**

* Users can link requirements to implementations using `implements:` macro
* Users can link requirements to tests using `tests:` macro
* Users can link requirements to documentation using `documents:` macro
* Users can link designs to requirements using `addresses:` macro (Phase 2)
* Users can link designs to other designs using `composed-of:` and `depends-on:` macros (Phase 2)
* Users can reference requirements inline using `req:` macro
* Circular references are detected and reported
* Invalid references are detected and reported
====

[req, id=REQ-003, title="Generate Traceability Matrices"]
====
The system shall generate traceability matrices showing relationships between requirements and their implementations.

**Acceptance Criteria:**

* Generate Requirements-to-Implementation matrix
* Generate Requirements-to-Test matrix
* Generate Full traceability matrix
* Matrices can be output as AsciiDoc tables
* Matrices can be output as HTML tables
* Matrices can be output as CSV files
* Matrices include coverage information
====

== Requirement Syntax

=== Block Macro

[source,asciidoc]
----
[req, id=REQ-005, title="Requirement Title", status=approved]
====
Requirement description goes here.

Can span multiple paragraphs.
====
----

=== Attributes

| Attribute | Description | Required | Default |
|-----------|-------------|------------|---------|
| id | Unique identifier | No | Auto-generated |
| title | Human-readable title | No | "Requirement \{id}" |
| status | Requirement status | No | draft |

=== ID Format

* Recommended: `\{TYPE}-\{NUMBER}` where TYPE is 2-4 uppercase letters
* Examples: `REQ-001`, `FUNC-001`, `EXAMPLE-001`, `DES-001`, `ARCH-001`
* Non-standard IDs (like `EXAMPLE-001` with 7+ letters) will generate a warning but still work
* All node types (requirements, implementations, tests, documents, designs) use the same ID format

== Next Steps

* Learn how these requirements are implemented in the xref:architecture.adoc[Architecture] section
* See the generated matrices in the link:traceability/index.html[Traceability] directory
* View the xref:matrices.adoc[Matrices] explanation
