site:
  title: Antora Tracer — Self-Traceability Example
  url: http://localhost:8080
  start_page: tracer::index.adoc

content:
  sources:
    - url: .
      start_path: examples
      branches: HEAD

# To enable live traceability generation during Antora builds,
# uncomment the extension and run `npm run build` first:
#
antora:
  extensions:
    - require: ./lib/src/antora-extension.js
      config:
        configPath: ./examples/traceability.yml
        outputDir: traceability
        generateMatrices: true
        matrixFormats: [html, csv]
        includeInNavigation: true
    - asciidoctor-kroki

ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true

output:
  dir: ./public
  clean: true
