## 1. Types

- [x] 1.1 Add `bidirectional?: boolean` field to `ItemRelationship` in `src/types.ts`

## 2. TraceabilityGraph — cleanup and helpers

- [x] 2.1 Remove dead `_inverseIndex` declaration and `clear()` call from `TraceabilityGraph`
- [x] 2.2 Add private `_removeFromIndex(relationship)` method that removes a relationship from `_relationshipIndex` and `_reverseRelationshipIndex`

## 3. Bidirectional merge logic

- [x] 3.1 Add inverse type resolution helper: resolve `inverseLabels` config first, then fall back to `INVERSE_MAP` from `types.ts`
- [x] 3.2 In `addRelationship`, before the duplicate check, look up the inverse key `targetId-invType-fromId`. If it exists, remove the inverse from storage and index, then mark the incoming relationship `bidirectional: true` and `inverseOf: <removed-id>`
- [x] 3.3 Skip merge when `invType` equals `relationship.type` (same-type pairs are intentional cycles, not bidirectional)

## 4. Circular reference detection

- [x] 4.1 In `findCircularReferences`, skip relationships where `rel.bidirectional === true` in addition to existing `rel.autoGenerated` skip

## 5. Graph visualization (toDot)

- [x] 5.1 In `toDot`, when rendering an edge for a relationship with `bidirectional: true`, use `dir=both` in the DOT edge attribute

## 6. Neo4j export

- [x] 6.1 In `Neo4jExporter`, include `bidirectional` as a boolean property on relationship rows in CSV and Cypher output when the flag is `true`

## 7. Tests

- [x] 7.1 Add test: inverse pair is merged (leads_to + is_derived_from → one bidirectional edge)
- [x] 7.2 Add test: single-direction relationship stored normally (no merge)
- [x] 7.3 Add test: no merge when inverse type is not configured
- [x] 7.4 Add test: same-type pair is NOT merged (depends + depends)
- [x] 7.5 Add test: INVERSE_MAP fallback works when no config inverseLabels
- [x] 7.6 Add test: bidirectional pair does not trigger circular reference error
- [x] 7.7 Add test: toDot renders `dir=both` for bidirectional edges
- [x] 7.8 Run `npm test` to verify all tests pass (279 baseline)
