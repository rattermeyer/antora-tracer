# Tasks: add-use-case-role

- [x] Create `use-cases.adoc` page with five UC items (UC-001 through UC-005), each with `leads_to:REQ-XXX[]` relationships
- [x] Add `use_case` role to `examples/traceability.yml`, `leads_to` relation, and `usecase-requirements` matrix
- [x] Add Use Cases link to `examples/modules/ROOT/nav.adoc` after Requirements
- [x] Rebuild and verify: UC items registered, relationships created, usecase-requirements matrix generated
- [x] Verify matrix shows UC → REQ coverage links and syncs to `_attachments/`
- [x] Copy static matrix attachment for nav resolution (nav runs before sitePublished)
