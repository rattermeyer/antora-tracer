# Tasks: config-driven-inverse-labels

- [x] Add `inverseLabels` to `TraceabilityConfig` interface
- [x] Add `inverseLabels` to `examples/traceability.yml`
- [x] Update `expandIncomingMacros()` to use config → INVERSE_MAP → raw type lookup chain
- [x] Build, test, verify inverse labels render correctly
